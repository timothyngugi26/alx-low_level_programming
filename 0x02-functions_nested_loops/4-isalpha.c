#include "main.h"

/** isaalpha checks for the alphabetic characters 
 * Return: 1 if c is a letter , 0 if otherwise
 */
int _isalpha(int c)
{
	return ((C >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
}
